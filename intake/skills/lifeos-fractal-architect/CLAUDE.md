# Claude home (`~/.claude`) — fractal entry

**Rule:** This file stays short. Universal routing only; no long workflows here.

## Universal heuristics

1. **Simplicity at the top, complexity at the bottom** — never invert. If a rule needs detail, put it in a child doc or skill.
2. **Progressive disclosure** — read the smallest file that answers the question; descend only when needed.
3. **Single owner per policy facet** — do not duplicate the same MUST/NEVER in multiple layers; link downward.

## Where to go next

| If you are… | Read next |
|-------------|-----------|
| Designing or operating LifeOS (Obsidian + GitHub + GCal + autonomy) | `lifeos-fractal-architect/SKILL.md` |
| Full architecture spec, runbook, harness detail | `notebooklm_lifeos_fractal_brief.md` (section *Authoritative architecture*) |
| Layer rubric, autonomy matrix, data templates | `lifeos-fractal-architect/references/` |

## Global harness

Stable toggles live in `settings.json` (env, effort). Do not move procedural LifeOS rules into `settings.json`.
